# HW9 JML 规格学习与实现验证平台

## 1. 平台简介

本平台面向 HW9 的 JML 学习、规格评测和受限实现验证。它把题目要求、学生当前提交、问题定位、OpenJML/RAC 运行证据和反例展示组织在同一个本地 Web 工作台中。

本次汇报重点不是“又增加了一个 JML 编辑页面”，而是围绕规格学习中的三个核心问题建立一条可操作的学习闭环：一致性很难直接证明，所以用数据点驱动测评；证明过程不能只给最终分数，所以提供交互式证明/验证；抽象诊断不容易理解，所以给学生可重跑的反例。

## 汇报重点：三项核心能力

### 1. 数据点驱动的测评

一致性证明在完整 JML 语义、任意对象状态和所有可能输入上都非常困难，直接追求“完整证明”既昂贵，也不适合学生练习的即时反馈。因此平台把课程组确认的参考 JML、领域状态和测试点组织成可执行的 Suite，在声明的有限语义域内进行确定性评测。

数据点不是随意样例，而是围绕规格义务设计的可解释状态：

- 正常行为和适用条件；
- 用户不存在、自我关注、重复关注等异常分支；
- 正确后态、漏更新、关系方向错误和无关修改；
- 当前支持范围内的 `assignable`、异常和状态变化。

评测器在相同数据点上解释参考合同和学生合同，比较行为分类、后置条件、异常匹配以及受支持的 frame 条件，并输出覆盖情况、问题类别和定位信息。这样平台可以稳定地产生分数和教学诊断，而不把一次难以完成的全局证明伪装成“已证明”。

这里的边界必须明确：数据点驱动测评是有限语义域内的可重复证据，不等于任意状态上的完整逻辑等价；“范围内未发现问题”也不等于完整正确性证明。

### 2. 交互式证明 / 验证

平台把证明任务拆成学生可以反复操作的闭环，而不是提交一次后只返回一个分数：

```text
阅读需求 → 填写一个规格义务 → 提交检查 → 查看问题和证据
    ↑                                      ↓
    └──────── 定位条款、修改草稿、再次提交 ────────┘
```

交互式工作流包括：

- 左侧题面、约束和条款导航；
- 中间逐空填写和完整 JML 预览；
- 右侧问题地图、诊断和证据摘要；
- 点击问题定位回对应空位；
- 草稿修改后自动标记旧结果过期；
- 在“实现验证”中选择运行案例、查看候选输入并切换证据详情；
- 通过提交轨迹回看不同版本的学习过程。

因此，学生不是被动接受结论，而是在“提出规格—检查义务—观察证据—修订规格”的过程中逐步完成证明。当前实现是面向课程的交互式验证工作台，并不声称已经提供 KeY 等完整交互式定理证明器或完整证明树。

### 3. 给学生可理解、可重跑的反例

仅告诉学生“第 2 个后置条件错误”，通常不足以帮助理解双向状态为什么不一致。平台在受限 Java 案例中进一步搜索小规模输入，并把确认后的反例展示成一份可以重新运行的问题案例：

- 具体参数，例如 `id1=0, id2=1`；
- 调用前状态；
- Java 实现产生的实际后状态；
- 规格要求的后状态；
- 违反的 JML 条款和参考位置；
- 独立重跑确认结果。

学生可以直接看到“following 已删除，但 followers 没有同步删除”这类状态差异。反例不是预设的错误答案，而是经过目标方法执行、RAC 检查和独立重跑确认的运行证据；它把抽象规格问题转化成可观察、可解释、可复现的学习材料。

三项能力的关系是：数据点保证测评可重复，交互过程让学生能够逐步修订，反例把失败原因具体化。平台最终形成“可测—可交互—可理解”的规格学习闭环。

平台当前包含两条相互独立的链路：

```text
官方完整 JML → 学生练习模板 → 学生填写 → JML–JML 确定性评测 → 学习反馈

教师配置的 Java 案例 → JML 绑定 → OpenJML ESC/RAC → 运行前后态观察 → 反例确认
```

第一条链路用于判断学生填写的规格是否与课程组批准的参考规格一致；第二条链路用于展示一个受限 Java 实现如何产生可复现的运行反例。两者的结论不会混为一个分数。

## 2. 主要功能

### 2.1 需求与规格上下文

工作台左侧显示当前练习的自然语言需求、正常行为、异常检查顺序和作答边界。学生可以通过条款导航回到对应的 JML 空位，减少在题面和代码之间来回查找的成本。

当前 `followUser` 练习要求描述：

- 正常调用的适用条件；
- 关注关系更新后的正向状态；
- 粉丝关系更新后的反向状态；
- 用户不存在、自我关注和重复关注等异常分支。

### 2.2 JML 逐空填写

中间区域提供以下交互：

- 只编辑课程组批准的 JML 空位；
- 实时生成完整 JML 预览；
- 载入公开错误样例；
- 重置当前草稿；
- 选择“提示”或“讲解”反馈深度；
- 提交当前规格进行审查。

接口签名、行为头、异常类型和其他锁定文本不会被学生编辑。未填写完空位时，平台只返回结构性提示，不进入语义评测。

### 2.3 确定性规格评测

提交后，服务器使用当前练习配置的 `semantic_suite` 调用 JML–JML 评测器，对参考合同和学生合同进行有限测试域上的比较。

评测可以报告：

- 未填写空位；
- 锁定框架被修改；
- 非法符号或格式错误；
- 正常行为条件不一致；
- 后置条件不一致；
- 异常条件重叠或缺失；
- 当前 Suite 未覆盖的范围。

评测结果会给出分数、诊断类别、问题位置和修正方向。这个分数表示在已声明测试域内与参考 JML 的一致程度，不是任意程序状态下的完整形式化证明。

### 2.4 结构化学习反馈

配置 `DEEPSEEK_API_KEY` 后，平台可以在确定性评测之后附加 Agent 学习反馈。反馈包括：

- 当前完成进度；
- 已正确的部分；
- 优先问题类别；
- 抽象反例形状；
- 下一步修正建议。

Agent 只解释服务器已经生成的公开诊断，不参与评分，也不接收教师参考答案、隐藏测试点或完整隐藏状态。提示模式只展示一个优先问题；讲解模式可以展示更多定位和抽象说明，但不直接给出完整参考 JML。

### 2.5 实现验证与反例

切换到“实现验证”后，可以运行教师配置的运行时案例。当前首个案例是：

> `unfollowUser` 只删除发起方的 following 关系，没有同步删除对方的 followers 关系。

平台会依次完成：

1. 构造小规模合法前态；
2. 绑定目标 JML；
3. 使用 OpenJML ESC/RAC 编译和执行；
4. 观察实际执行前后的抽象关系；
5. 根据绑定规格确认违反项；
6. 在独立执行阶段重新运行同一输入。

确认后的反例页面展示：

- 候选输入名称；
- `id1`、`id2` 等方法参数；
- 前态、实际后态和要求状态；
- 违反的 JML 条款及错误代码；
- 绑定规格中的参考位置；
- 独立重跑确认结果；
- 完整状态快照和验证会话证据。

只有目标方法确实执行、违反来自绑定规格且独立重跑成功时，页面才显示“已复现违反”。“范围内未找到”只代表当前有限搜索没有找到反例，不代表实现正确。

### 2.6 问题地图与证据联动

右侧“结果与问题”区域把当前提交的证据集中展示：

- 当前规格状态；
- 确定性评测结果；
- 静态证明状态；
- 运行复现状态；
- 问题列表；
- 当前选中问题的解释和修正建议。

点击问题后，可以查看具体条款，并定位回中间区域对应的填写位置。编辑草稿后，旧结果会被标记为过期，避免用户把旧反馈误认为当前草稿的结果。

### 2.7 提交与工具轨迹

底部轨迹区域记录本地会话中的提交版本，包括：

- 版本号；
- 反馈模式；
- 提交时间；
- 结果是否仍与当前草稿一致。

该记录用于学习过程回看，不是持久化成绩数据库，也不承担课程身份认证和权限管理。

### 2.8 双一致性教学演示

平台保留一个独立的“双一致性教学演示”入口，用于说明以下两条关系：

```text
自然语言题意 → 学生 JML
学生 JML → Java 实现
```

这个演示可以帮助说明：仅检查 Java 是否满足学生自己写的 JML 并不足以证明规格正确。因此它与普通 JML–JML 提交评分、运行时反例验证分别显示。

## 3. 页面结构

宽屏下工作台采用三栏布局：

| 区域 | 内容 |
| --- | --- |
| 01 · Context | 需求、约束、条款导航、学习提示 |
| 02 · Write / Validate | JML 填写或运行时验证 |
| 03 · Evidence | 评分、问题、运行状态、反例证据 |
| 04 · Trace | 提交版本和工具轨迹 |

在窄屏下，三栏会按顺序堆叠。候选输入和反例详情内部允许长名称、错误码和状态快照换行，避免跨栏遮挡或产生横向溢出。

## 4. 运行平台

### 4.1 启动 Web 服务

在仓库根目录进入 `Agent/` 后运行：

```powershell
Set-Location "Agent"
python -m hw9_agent web `
  --exercise-dir "exercises\follow_user" `
  --host 127.0.0.1 `
  --port 8000
```

浏览器打开：<http://127.0.0.1:8000>

指定 OpenJML 路径时，可以使用参数或环境变量：

```powershell
$env:OPENJML = "wsl:/home/ranye/.local/openjml-21.0.27/openjml"
```

或者：

```powershell
python -m hw9_agent web `
  --exercise-dir "exercises\follow_user" `
  --host 127.0.0.1 `
  --openjml "wsl:/home/ranye/.local/openjml-21.0.27/openjml" `
  --port 8000
```

不配置 DeepSeek 密钥也可以使用题面、逐空填写、结构检查和确定性规格评测。只有需要 Agent 学习建议时才设置：

```powershell
$env:DEEPSEEK_API_KEY = "你的密钥"
```

不要把 API 密钥写入仓库、练习包或文档。

### 4.2 服务端接口

| 方法与路径 | 功能 | 是否调用模型 |
| --- | --- | --- |
| `GET /health` | 检查服务是否正常 | 否 |
| `GET /api/exercise` | 获取公开题面、模板、样例和反馈契约 | 否 |
| `POST /api/check` | 执行结构检查和确定性评测 | 否 |
| `POST /api/review` | 提交审查并可附加 Agent 反馈 | 仅配置密钥时 |
| `POST /api/demo-consistency` | 运行双一致性教学案例 | 否，但案例会调用 OpenJML |
| `POST /api/runtime-demo` | 运行教师配置的 RAC 反例案例 | 否，但案例会调用 OpenJML/RAC |

浏览器不能通过这些接口提交任意服务器路径、任意 Java 文件或任意命令。运行时案例使用服务端预先配置的资产。

## 5. 评测结论如何理解

平台刻意区分不同证据：

| 状态 | 含义 |
| --- | --- |
| `PROVED` | 当前静态工具在其检查范围内证明通过 |
| `UNPROVED` | 静态检查未能证明通过，不单独等同于实现错误 |
| `UNKNOWN` | 工具或语义范围不足以给出结论 |
| `REPRODUCED` | 运行方法触发了违反项，并完成独立重跑确认 |
| `NOT_FOUND_WITHIN_BOUNDS` | 当前有限候选范围内没有找到反例 |
| `ERROR` | 编译、运行、配置或基础设施错误 |

特别是：

- JML–JML 评分不是完整证明；
- OpenJML RAC 只检查实际执行到的路径；
- 有限搜索没有找到反例，不代表实现正确；
- 基础设施错误不应被折算为学生规格得分为零；
- `assignable`、异常行为和输出约定的支持范围以当前 Suite/适配器能力清单为准。

## 6. 当前范围与限制

当前版本已经支持学习工作台、JML–JML 确定性评测、结构化反馈、教师配置的 `unfollowUser` 运行反例、OpenJML/RAC 证据和本地版本轨迹。

以下功能暂不属于当前运行时里程碑：

- 任意学生 Java 或 ZIP 上传执行；
- 面向任意对象图的自动适配；
- `followUser` 的通用运行时检查；
- 反例自动缩减和 JUnit 一键导出；
- KeY、完整证明树或完整 JML 语义支持；
- 多用户认证、权限系统和成绩数据库；
- 将 WSL 当作完整安全沙箱。

因此，实现验证入口当前是教师侧、本地、窄接口的教学案例，不是面向不受信任代码的通用在线评测沙箱。

## 7. 代码组织

| 文件或目录 | 职责 |
| --- | --- |
| `Agent/web/index.html` | Web 工作台结构和交互区域 |
| `Agent/web/app.js` | 页面状态、提交、问题联动和反例展示 |
| `Agent/web/styles.css` | 三栏布局、响应式样式和证据卡片 |
| `Agent/hw9_agent/webapp.py` | Web API、练习加载、评测调用和案例边界 |
| `Agent/exercises/follow_user/` | 学生题面、模板、公开样例和练习配置 |
| `judge-2027/unit3/spec_judge/semantic_check.py` | JML–JML 确定性语义评测 |
| `judge-2027/unit3/spec_judge/runtime_verifier.py` | 受限 Java RAC 反例搜索与确认 |
| `judge-2027/unit3/spec_judge/verification_evidence.py` | 验证会话和证据记录模型 |
| `judge-2027/unit3/spec_judge/suites/` | 方法、Profile、参考合同和测试点配置 |

## 8. 发布前检查

Agent 回归测试：

```powershell
Set-Location "Agent"
python -m unittest discover -s tests -p "test_*.py"
node --check web\app.js
python -m py_compile hw9_agent\webapp.py
```

新增或修改 Suite 后，还应运行对应的 Suite 校验和 `judge-2027/unit3/spec_judge` 回归测试。发布前必须确认参考 JML、隐藏测试点和教师资产没有进入学生包或浏览器公开响应。
